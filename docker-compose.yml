services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: fantasyworld
      POSTGRES_PASSWORD: fantasyworld
      POSTGRES_DB: fantasyworld
    ports:
      - "5432:5432"
    volumes:
      - fantasyworld-postgres:/var/lib/postgresql/data

volumes:
  fantasyworld-postgres:
